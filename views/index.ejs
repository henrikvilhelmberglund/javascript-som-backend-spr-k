<%- include("header") %>
<h1><%= title %></h1>
<p><%= message %></p>
<ul>
	<% users.forEach((user) => { %>

	<li><%= user.id %> : <%= `${user.name} (${user.email})` %></li>
	<% }) %>
</ul>

<%- include("footer") %>
