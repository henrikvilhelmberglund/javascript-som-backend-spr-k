<html>
	<head>
		<title><%= title %></title>
	</head>
	<body>
		<h1><%= title %></h1>
		<p><%= message %></p>
		<ul>
			<% users.forEach((user) => { %>

			<li><%= user.id %> : <% user.name %></li>
			<% }) %>
		</ul>
	</body>
</html>
